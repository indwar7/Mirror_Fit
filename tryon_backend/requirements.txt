fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
websockets==12.0
torch>=2.3.0
torchvision>=0.18.0

# diffusers with ControlNet extras (AnimateDiff + ControlNet pipelines included)
# Pinned exact — matches instantid_backend/liveportrait_backend and the last
# known-working stack. Unbounded >= here pulled diffusers 0.37 / transformers 5.6 /
# huggingface_hub 1.14 on a fresh install, which broke CatVTON + SD-Inpainting
# pipeline loading (silent fallback to the plain img2img tier — no garment mask).
diffusers[torch]==0.31.0

transformers==4.46.0
accelerate==1.1.0
peft==0.13.2
opencv-python-headless>=4.9.0.80
Pillow==10.4.0
numpy==1.26.4
huggingface_hub==0.26.2
safetensors==0.4.5
mediapipe>=0.10.0

# Phase 3 — DWPose body-pose conditioning via ControlNet
# DWposeDetector + all pose/depth annotators in one package
controlnet_aux>=0.0.7

# Phase 2 — AnimateDiff video backbone (included in diffusers>=0.29 but listed explicitly)
# AnimateDiffPipeline, MotionAdapter are part of diffusers — no extra package needed.
# Install xformers separately for ~20% faster attention (optional):
#   pip install xformers --index-url https://download.pytorch.org/whl/cu121

# Flash Attention 2 (needs CUDA headers — install separately):
#   pip install flash-attn --no-build-isolation

# TensorRT (install from NVIDIA wheel matching your CUDA version):
#   pip install tensorrt==8.6.1 --index-url https://pypi.ngc.nvidia.com
#   pip install cuda-python
