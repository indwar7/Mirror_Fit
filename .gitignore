# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
.codegraphy/

# Unrelated projects that happen to share this working tree. `git add -A`
# swept all three into a Mirror Fit commit (7b0a62c) once already; the first
# two are separate repos with their own remotes, so they landed as broken
# gitlinks rather than content.
/transcript-api/
/transcript-web/
/ncert-content/

# Local Claude Code settings — machine-specific, not project config.
/.claude/settings.json

# macOS duplicates Finder leaves behind ("file 2", "file 3").
* [0-9]
